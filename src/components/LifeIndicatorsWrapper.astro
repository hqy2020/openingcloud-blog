---
/**
 * LifeIndicators Astro wrapper
 */
import { LifeIndicators } from '@/react/sections/LifeIndicators';

interface Props {
  stats?: {
    steps: number;
    sleep: number;
    focus: number;
    exercise: number;
  };
}

const { stats } = Astro.props;
---

<LifeIndicators client:visible stats={stats} />
