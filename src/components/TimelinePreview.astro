---
/**
 * 人生路线预览 — 非线性磁贴 + CTA
 * Phase 3 会从 D1 读取真实数据
 */

const milestones = [
  { year: '2020', title: '大学入学', type: 'learning', icon: '🎓', desc: '开始系统化学习与项目实践' },
  { year: '2023', title: '第一份实习', type: 'career', icon: '💼', desc: '进入真实业务环境，积累工程经验' },
  { year: '2025', title: '启云博客上线', type: 'career', icon: '🚀', desc: '形成公开表达与知识沉淀闭环' },
];

const typeColors: Record<string, string> = {
  career: 'bg-primary-100 text-primary-600 dark:bg-primary-900/30 dark:text-primary-300',
  learning: 'bg-grass-50 text-grass-600 dark:bg-grass-700/20 dark:text-grass-300',
  health: 'bg-sun-100 text-sun-600 dark:bg-sun-600/20 dark:text-sun-300',
  family: 'bg-primary-50 text-primary-500 dark:bg-primary-900/20 dark:text-primary-400',
};

const cardLayouts = [
  'col-span-12 md:col-span-4 md:translate-y-7',
  'col-span-12 md:col-span-5 md:-translate-y-4',
  'col-span-12 md:col-span-3 md:translate-y-5',
];
---

<section class="mb-0" data-animate="fade-up">
  <div class="relative overflow-hidden rounded-[1.75rem] border border-slate-200/70 dark:border-slate-700/60 bg-white/75 dark:bg-slate-900/70 p-5 md:p-6">
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_90%_20%,rgba(58,154,255,0.14),transparent_38%),radial-gradient(circle_at_8%_86%,rgba(255,184,107,0.17),transparent_36%)]"></div>
    <div class="relative z-10">
      <div class="flex items-center justify-between gap-4 mb-4">
        <h2 class="text-xl md:text-2xl font-display text-slate-800 dark:text-slate-100">人生路线</h2>
        <a href="/timeline/" class="text-sm text-primary-500 hover:text-primary-600 transition-colors">
          查看完整时间线 →
        </a>
      </div>

      <div class="grid grid-cols-12 gap-3 md:gap-4">
        {milestones.map((m, i) => (
          <a
            href="/timeline/"
            class:list={[
              cardLayouts[i % cardLayouts.length],
              'group relative block rounded-2xl p-4 md:p-5 border border-slate-200/75 dark:border-slate-700/65 bg-white/80 dark:bg-slate-900/80 hover:shadow-soft-md transition-all duration-300 hover:-translate-y-1',
            ]}
          >
            <div class="flex items-start justify-between gap-3">
              <span class="text-2xl">{m.icon}</span>
              <span class="text-xs px-2 py-0.5 rounded-full bg-slate-100 dark:bg-slate-800 text-slate-500 dark:text-slate-300">{m.year}</span>
            </div>
            <h3 class="font-semibold text-slate-800 dark:text-slate-100 mt-3">{m.title}</h3>
            <p class="text-xs text-slate-500 dark:text-slate-400 mt-1 line-clamp-2">{m.desc}</p>
            <span class:list={['mt-3 inline-block text-xs rounded-full px-2 py-0.5', typeColors[m.type]]}>
              {m.type}
            </span>
          </a>
        ))}
      </div>
    </div>
  </div>
</section>
