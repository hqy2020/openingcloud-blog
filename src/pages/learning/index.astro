---
import BaseLayout from '@/layouts/BaseLayout.astro';
import MagazineLayout from '@/components/MagazineLayoutWrapper.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('learning', ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const postsForLayout = posts.map(post => ({
  title: post.data.title,
  description: post.data.description,
  date: post.data.date,
  category: post.data.category,
  tags: post.data.tags,
  cover: post.data.cover,
  slug: post.id,
}));
---

<BaseLayout title="效率方法" sidebar={false}>
  <MagazineLayout
    posts={postsForLayout}
    title="效率方法"
    subtitle="高效学习策略、认知科学与个人成长"
  />
</BaseLayout>
